#include <iostream>

using namespace std;

int main()
{
    int x;
    cout << " enter a number: ";
    cin >> x;
    if ( x > 100)
        cout << " that number is greter than 100 ";
    else
        cout << " that number is less than 100 ";
    return 0;
}
